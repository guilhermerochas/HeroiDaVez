import 'package:flutter/material.dart';

class IncidentCardItem extends StatelessWidget {
  const IncidentCardItem({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text(
      "Hello World",
    );
  }
}
